services:
  nodered:
    image: nodered/node-red:latest
    container_name: my-nodered
    restart: unless-stopped
    ports:
      - "1880:1880"
    volumes:
      - ./node-red-data:/data
    environment:
      - TZ=Europe/Moscow